spring:
  application:
    name: kafka-to-elasticsearch
  profiles:
    active: kafka_to_elasticsearch
  cloud:
    config:
      name: kafka-to-elasticsearch,config-client
      uri: http://localhost:8888
      username: spring_cloud_user
      password: pass